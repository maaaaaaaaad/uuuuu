import 'package:flutter_test/flutter_test.dart';
import 'package:jellomark/features/beautishop/domain/repositories/beauty_shop_repository.dart';

void main() {
  group('BeautyShopRepository', () {
    test('defines getNearbyShops method', () {
      expect(BeautyShopRepository, isNotNull);
    });

    test('defines getRecommendedShops method', () {
      expect(BeautyShopRepository, isNotNull);
    });

    test('defines getDiscountShops method', () {
      expect(BeautyShopRepository, isNotNull);
    });

    test('defines getNewShops method', () {
      expect(BeautyShopRepository, isNotNull);
    });
  });
}
